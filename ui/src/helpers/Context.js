import React from 'react';

export const WSClientContext = React.createContext(undefined);
export const CurrentTracking = React.createContext(undefined);
export const DisplayedValue = React.createContext(undefined);
export const Web3 = React.createContext(undefined);
export const ZoneData = React.createContext(undefined);
