import React from 'react';

export const WSClientContext = React.createContext(undefined);
export const CurrentZoneHash = React.createContext(undefined);
export const DisplayedValue = React.createContext(undefined);
